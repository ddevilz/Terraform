# Terraform local directories (providers, modules, plugin cache)
**/.terraform/*

# Terraform state files
terraform.tfstate
terraform.tfstate.*

# Terraform state files anywhere in the repo (defense-in-depth)
**/*.tfstate
**/*.tfstate.*

# Crash logs
crash.log
crash.*.log

# Sensitive variable files (may contain secrets)
*.tfvars
*.tfvars.json
